#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - computes string length
 * @s: string whose length to be measured
 * Return: length of string
 */

int _strlen(char *s)
{
	return (strlen(s));
}
