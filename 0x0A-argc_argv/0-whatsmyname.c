#include "main.h"
#include <stdio.h>

/**
 * main - main function
 *
 * @argc: number of argumbers
 * @argv: array
 * Return: 0 to terminate code
 */

int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", *argv);

	return (0);
}
