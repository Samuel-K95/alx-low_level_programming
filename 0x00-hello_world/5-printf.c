#include<stdio.h>

/**
 * main - driver function
 * Description: prints using printf
 * Return: returns 0 to terminate code
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
