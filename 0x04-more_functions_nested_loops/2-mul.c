#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two integers
 * Description: multiplies two numbers
 * @a: first number
 * @b: second number
 * Return: the multiplied number
 */

int mul(int a, int b)
{
	return (a * b);
}
